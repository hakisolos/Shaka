{
  "name": "shaka",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@discordjs/voice": "^0.17.0",
    "@types/node": "^18.0.6",
    "bufferutil": "^4.0.8",
    "discord.js": "^14.16.3",
    "ironman-api": "^2.0.1",
    "dotenv": "^16.0.0"  // Added dotenv for environment variable management
  },
  "devDependencies": {
    "@types/dotenv": "^16.0.0"  // Optional: added for TypeScript support if needed
  }
}
